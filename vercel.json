{
  "buildCommand": "npm install --workspace=packages/frontend && npm run build --workspace=packages/frontend",
  "outputDirectory": "packages/frontend/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
