{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd packages/backend && npx prisma generate && npm run build"
  },
  "deploy": {
    "startCommand": "cd packages/backend && npx prisma db push --accept-data-loss --skip-generate && node dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
